name: sunmi_utils
description: "Scanner and printer utilities for Sunmi POS devices: barcode scanner stream, and printing text, QR codes, barcodes, tables, and images on the built-in Sunmi printer."
version: 0.1.0
repository: https://github.com/adiwirya/sunmi_utils
topics:
  - sunmi
  - printer
  - scanner
  - pos
  - barcode

environment:
  sdk: ^3.12.1
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0

flutter:
  plugin:
    platforms:
      android:
        package: com.adiwirya.sunmi_utils
        pluginClass: SunmiUtilsPlugin
